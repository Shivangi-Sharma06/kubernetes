 apiVersion: v1
 kind: Service
 metadata:
   name: cluster-svc
   labels:
     env: demo
 spec:
  type: ClusterIP 
  ports:
    - port: 80  
      targetPort: 80
  selector:     # we donot use matchLabel feild unline in deployment and rc rs
   env: demo 
