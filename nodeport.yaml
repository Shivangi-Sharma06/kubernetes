 apiVersion: v1
 kind: Service
 metadata:
   name: nodeport-svc
   labels:
     env: demo
 spec:
  type: NodePort
  ports:
    - nodePort: 30001  
      port: 80
      targetPort: 80
  selector:     # we donot use matchLabel feild unline in deployment and rc rs
      env: demo 
